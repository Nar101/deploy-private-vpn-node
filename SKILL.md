---
name: deploy-private-proxy-node
description: >
  帮助不懂服务器的新手比较并购买海外 VPS，部署只供自己使用的 VLESS Reality Vision 私人节点，并在 Shadowrocket、Clash/Mihomo、v2rayN、sing-box 等客户端完成原生接入、独立分流、真实验收、排障、备份和跨设备交付。适用于新购、复制、节点超时、客户端适配、安全维护与换机；开始前必须讲清费用，付款和关键网络变更必须由用户确认。仅限个人或极少数受信任设备的合法使用，不用于机场、多用户售卖或公开订阅。
---

# 搭建私人魔法节点

把结果交付到“用户自己的客户端真实可用”，不止是服务器安装成功。按交付门推进；前一门没有证据，不得跳到后一门或报告完成。

## 先确定模式与证据等级

- `new-deploy`：采购到完整交付。
- `clone`：复制已验证架构，但重新生成全部实例凭证。
- `diagnose`：先收集失败分层证据，不擅自重装。
- `maintain`：备份、升级、换 IP、恢复或收紧暴露面。
- `client-adapt`：服务端已可用，只做新客户端或新设备接入。

开始时报告当前门、下一门和支持等级：

- `A 已实测`：相同操作系统、客户端和协议组合真实通过。
- `B 文档核对`：官方能力已确认，当前设备尚未跑通。
- `C 标准引导`：只满足能力契约，具体实现需要现场验证。

支持等级描述证据，不代表成功率。用户不懂技术时给一个主推荐及理由，不把协议参数变成用户必须先学会的选择。

## 不可突破的边界

- 仅供个人或极少数受信任设备使用；不开放注册、不售卖、不搭机场。
- 不在对话、仓库、普通日志或文档写入 SSH 私钥、密码、UUID、Reality 私钥、short ID、面板 Token 或完整分享 URI。
- 付款、实名、验证码、自动续费、最终下单和关键网络授权始终由用户完成。
- 不改变当前全局代理、默认路由或正在使用的节点，除非动作发生前得到明确确认。
- 不把数据中心 IP 表述为住宅 IP，不保证永久干净、永久可访问或所有线路稳定。
- 不盲跑来源不明的一键脚本；优先官方项目、固定版本、变更前备份和变更后回读。
- 安全设置、云防火墙、VPN 路由、服务重启和永久删除临时资产按当前工具确认策略执行。

## G0 采购已确认

需要新服务器时完整读取 `references/provider-selection.md`，从当前官方页面核对价格，不从记忆报现价。

采购前至少交付：

1. 极限低价、综合性价比、稳定优先三个满足硬门槛的候选；不足三个时说明市场缺口。
2. 首购、续费、币种、付款周期、自动续费、独立 IPv4、换 IP、流量、税费和退款条件的同口径比较。
3. 一个结合用户所在地、运营商、设备、主要服务、流量和预算的主推荐。
4. 明确说明买到的是空白 VPS，峰值带宽不是稳定吞吐承诺，目标服务和晚高峰必须实测。

每次付款前再次用一句话说明“买什么、为什么、现在付多少、以后如何收费”。用户完成付款后才进入 G1。

## G1 实例与 SSH 已就绪

1. 将新私钥只保存在本机安全路径，权限 `600`；AI 只引用路径，不回显内容。
2. 用 `scripts/audit-server.sh` 或同等只读命令核对系统、资源、服务、端口、UFW、SSH、BBR、Fail2ban 和更新真相。
3. 回读云厂商外层防火墙；服务器监听和 UFW 都不能替代它。
4. 记录非敏感基线：供应商、地区、OS、资源、到期日、流量、版本和公开端口。
5. 脏系统先备份，不覆盖现有业务。

完成证据：实例 Running、规格符合订单、SSH 密钥可重复登录、外层防火墙可回读。

## G2 服务端基线已完成

默认只部署 `VLESS + Reality + Vision / TCP 443`：

1. 核对并固定 Xray 与 3X-UI 官方版本；面板只负责管理，不向公网开放。
2. 每个实例生成新的 UUID、X25519 密钥和 short ID，不复用旧实例凭证。
3. Reality target/SNI 必须从服务器验证 TLS 1.3、证书和稳定可达性；不能只因域名知名就采用。
4. 使用 `flow=xtls-rprx-vision`、TCP 443，并回读 Xray 实际加载的有效配置，不只看 API 提交内容。
5. 运行 `scripts/preflight-reality.sh --config <effective-config.json>`。客户端为 Shadowrocket 或其他非同版本 Xray 实现时，同时读取 `references/reality-compatibility.md`。
6. 云防火墙、UFW、实际监听保持一致；公网默认只开放 SSH 与 TCP 443。
7. 验证新会话的 SSH 密钥登录后，再禁 root 和密码登录。补丁升级若涉及 systemd、OpenSSH、Netplan、nftables 或内核，移到维护窗口。

完成证据：Xray/3X-UI active、配置校验通过、正式 443 由预期进程监听、两层防火墙一致、管理和订阅端口未公开。

腾讯云轻量服务器还要读取 `references/tencent-lighthouse.md`；零断流与安全口径读取 `references/security-and-acceptance.md`。

## G3 Reality 公网协议已通过

先用与当前全局代理隔离的 Xray/sing-box 客户端验证真实公网路径，再进入 GUI 客户端排障：

1. 确认连接新服务器 IP 的系统路由不会再次进入当前 TUN。
2. 通过独立本地 SOCKS/HTTP 入口验证新 VPS 出口、Google 204、OpenAI/Claude HTTP 响应。
3. 失败时先证明服务端是否收到连接，再按 `references/troubleshooting.md` 分类；没有分层证据，不得换端口、核心版本、指纹或协议。

完成证据：独立客户端经公网完成 Reality/VLESS 握手，出口 IP 等于新 VPS，至少一个 HTTP 目标返回预期状态。

独立客户端成功只证明“协议可用”，不能替代 G4 的原生客户端验收。

## G4 原生客户端已可用

先读取 `references/client-adapters.md`，再按客户端读取专项 reference：

- Shadowrocket：`references/shadowrocket.md`，macOS 路径为 `A 已实测`。
- Clash/Mihomo：`references/clash-mihomo.md`，当前为 `B 文档核对`。
- Windows：`references/windows.md`，v2rayN/Mihomo 当前为 `B/C`。
- 其他客户端：现场核对 VLESS、Reality、Vision、TCP、本地代理/TUN 和可回读日志；缺能力就换客户端，不削弱服务端协议。

通用顺序：

1. 记录当前节点、全局路由、规则文件、代理端口与回退方式。
2. 导入新节点，但不切换当前全局节点。
3. 回读客户端保存态中的地址、端口、flow、SNI、public key、short ID 和 fingerprint；只报告非敏感字段或哈希。
4. 执行客户端自己的节点测试，并用服务端到包或客户端运行日志证明运行态确实加载了新节点。
5. 客户端保存态正确但运行态仍旧时，先找公开热重载入口；需要重建隧道时，取得用户对短暂断流的明确授权并准备自动恢复。

完成证据：原生客户端节点测试成功，或其运行日志明确完成 Reality/VLESS 连接。独立 Xray 成功不能冒充本门完成。

## G5 独立分流与真实业务已通过

不切换全局节点。建立一个只包含新节点的临时验收组，并把少量精确验收域名置顶送入该组：

1. 从运行态读取真实本地代理端口，不假定 `1082`、`7890` 或 `10808`。
2. 使用 `scripts/verify-client.sh --proxy <proxy> --expected-ip <VPS_IP>` 验证出口、Google、OpenAI 和 Claude。
3. 做真实下载时，为测速域名单独置顶规则；旧域名规则抢先命中时，该速度成绩无效。
4. 从客户端运行日志同时证明“域名 → 验收组 → 新节点”，不能只凭请求成功推断路由。
5. 至少再覆盖一个真实应用或第二网络环境；晚高峰要求只能在真实时间窗口完成。

完成证据：原生客户端、独立分流、出口 IP、目标服务、规则命中全部成立。带宽只报告实测值和测试条件，不拿峰值宣传替代。

## G6 收尾、备份与跨设备交付已完成

1. 删除临时诊断入站、客户端节点、规则和两层防火墙端口；回读正式 443 所属进程。
2. 运行 `scripts/backup-xui.sh <output-file>`，核对远端与本地 SHA-256 后删除服务器临时副本。
3. 使用 `references/device-handoff.md` 和 `scripts/build-transfer-package.sh` 生成节点文件与规则文件分离的受限传输包；不包含 SSH 私钥。
4. 每台新设备独立完成原生节点测试与出口验证，不把“文件已传到”报告为“设备已可用”。
5. 明确是否真的提供公网订阅；单节点 URI 不是订阅。
6. 分别报告本地保存、GitHub 同步和设备导入状态；没有远端证据就不说已同步。

## 故障止损协议

- 每次只改变一个变量；改前记录状态，改后回读真实生效对象。
- 失败后恢复原值，并证明正式服务、端口和客户端记录已恢复。
- 测试若被端口残留、规则顺序、隧道缓存或时间窗口错位污染，标记无效，不进入结论。
- 连续三个假设失败，停止修改，按 `references/troubleshooting.md` 重建证据表。
- 没有新的服务器到包或协议日志证据时，不重复更换指纹、端口、核心版本、target 或协议。
- 会占用正式 443 的调试只能使用 `scripts/run-debug-window.sh` 或同等服务器本地看门狗；传入有效配置用于日志脱敏，不得依赖 SSH 会话存活来恢复。

## 完成状态必须分层报告

- `服务端就绪`：服务、配置、端口和防火墙成立。
- `协议可用`：独立客户端公网握手成立。
- `原生客户端可用`：用户实际客户端连接成立。
- `独立分流可用`：指定业务真实命中新节点。
- `迁移可完成`：真实应用、速度、目标网络/晚高峰和跨设备达到用户门槛。

只有最后一层完成，才能建议停用旧节点。退款、销毁或关闭旧节点始终需要用户本人确认。

## 版本与适用性

当前 `v0.5.0` 将购买后交付重构为七个证据门，加入 Reality 客户端版本兼容预检、Shadowrocket 四层真相、防回环、独立分流、自动恢复调试窗口和跨设备文件交付。腾讯云 Ubuntu 24.04、3X-UI/Xray、VLESS Reality Vision、macOS Shadowrocket 的东京与硅谷部署已有真实案例；线路稳定性仍按地区和运营商现场验证。Mihomo 与 v2rayN 为 `B/C`，其他组合不得宣称 100% 兼容。
